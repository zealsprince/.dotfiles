#!/usr/bin/env bash
# Stream Lambda logs using AWS CLI v2 (no awslogs dependency).
# Usage:
#   lambda-logs            # tails /aws/lambda/$PWD
#   lambda-logs my-func    # tails /aws/lambda/my-func

fn="${1:-${PWD##*/}}"
aws logs tail "/aws/lambda/${fn}" --since 10m --follow
